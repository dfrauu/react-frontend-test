import React from "react";
import TodosList from "./components/TodosList"; // Import the component

const App = () => {
  return (
    <div>
      <TodosList />
    </div>
  );
};

export default App;
